验证日期2026-09-22。VERIFIED_LOCAL：Node v22.22.2，nghttp2 1.64.0，Darwin27.0.0 arm64；单127.0.0.1随机服务，无TLS/ALPN，无故障注入。
node loopback.mjs > loopback.jsonl：exit0。一个server session，两流id1/3，双方请求到齐再发响应；慢流先16B保持开放，快流FAST-26全部7B被客户端接收后才释放慢流剩余65520B。最终慢流65536B全为83，两正文精确比较及SHA256。控制逻辑决定完成顺序，不归因默认优先级。
localSettings事件后local initialWindowSize1024，服务端连接remoteWindowSize65535；未连续采样窗口，未解析WINDOW_UPDATE，不声称实际流控耗尽时长。HPACK仅session.state快照，未解码线上字段块。
handcalc.py为离线窗口扣减与有限HPACK例子，不是通用编解码器、协议实现或真实网络证据。初始conn12/stream1=8/stream3=10，发送8/4后conn0；只增stream1不恢复conn额度；conn再增5才可发5。HPACK固定authority例子17字节，表记账57，后续索引be依赖保留上下文。
隔离环境探测：现有podman-machine-default在同shell启动、SSH探测并用EXIT trap停止。VM PATH无node/nghttp/nghttpd，/usr/bin/python3的find_spec(h2)=None。4个已有镜像0b78cd330150、32d322b19846、92947e844d1b、4607a309caf9，以--pull=never --network=none --read-only --entrypoint /bin/sh --rm运行command -v node/nodejs/nghttp/nghttpd/python3均无输出；后两镜像amd64与arm64有架构警告，探测exit0。未全盘搜索镜像，不宣称不存在任意非PATH二进制。
没有下载镜像、安装依赖、修改宿主默认路由或运行ds13-parent-check。VM最后stopped successfully。真实隔离TCP丢包验收NOT_RUN，不能用本机并发替代；待具备成熟H2的专用Linux环境补双端捕获、定向丢包、重传及应用交付对齐。
Node只使用内建http2/events/assert/crypto/os；8秒超时，最终destroy本次sessions与close服务。Python标准库；无额外LSP工具安装或类型检查成功声明。
一手资料RFC9113§1/3.3/4.1/4.3/5/5.2/5.3/6.9；RFC7541§2.3/4.1/C.3；Node官方22.22.2文档。
