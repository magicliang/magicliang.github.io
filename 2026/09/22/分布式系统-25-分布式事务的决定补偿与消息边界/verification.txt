第25篇验证记录，2026-09-22

正式源码SHA256 0642ba54766c277a8796bbb755e8c1804019d0a31b94cc8aac3783114d7c4a46
正文SHA256 18684130e4058b4d1aba868dbbca7058e1965c6de3935350994605601bfe7140

作者运行：/usr/bin/python3 -B transactions25/check.py，Python3.9.6/SQLite3.54.0，exit0；CLI --help exit0，--unexpected exit2。
父独立运行：python3 -B examples/distributed-systems/transactions25/check.py，Python3.14.4/SQLite3.51.3，session23834/chunkf71919 exit0。
两轮均显式TMPDIR/TMP/TEMP=仓库examples/distributed-systems/.build/transactions25/tmp。
作者run-6aaf2642f2bc469abd95b4a8a62b99bb；父run-e237e088cc65411fb29a9c7a58981abf。
13个SQLite场景，9组不同PID的73→0恢复；两轮忽略PID摘要一致。原始父事件随observations.json.txt持久保存。
父进程新连接先观察并可能首先完成回滚恢复，新子进程继续业务。DELETE/FULL/MEMORY逐连接设置回读。

双扫描：tone profile tech，4758汉字，0错误0提醒；随后persona人工全文+关键词0命中。源码审查在运行前完成，正式实验没有新编译二进制或系统tmp程序。

首次构建：npm run build，session92855/chunk2f20e1 exit0；2487文件/6.07s。
浏览器：本地127.0.0.1:4107实际页面，9个Mermaid SVG有非零尺寸，无Syntax error，初次载入控制台error=[]；正文、实验结果表和三附件链接可见。滚轮落在SVG会触发既有缩放，重新载入后沿页边滚动可恢复；未修改全站缩放组件。
素材/链接：三生成附件逐字与源一致，封面存在，两站内post_link目标存在。
最后仅修改Mermaid代码块的三处长标签换行以提高字号，属于代码块局部编辑，按AGENTS双扫描豁免。最终构建和布局复查结果见STATUS.md。
未验证附件的浏览器下载交互；已核对生成字节。

证据限于本地SQLite进程切点及有限2PC轨迹；未验证真实XA、broker、网络分区、任意崩溃、断电、并发Relay或生产性能。

最终页面复查：9SVG仍有效，复制层图viewBox由1182×350缩为894×350，标签可读。图形之外发现既有目录问题：点击目录触发utils.js getEleTop空元素getBoundingClientRect错误。25及24生成HTML的toc-link均无href，主题main.js却读取href查目标；属于站点现有交互缺口，本批未改全站组件或public。目录点击未通过，不宣称整页全部交互无错。
最终内容构建session9813/chunkb23591 exit0，2395文件/6.81s；补录本发现后再次构建结果见STATUS.md。
