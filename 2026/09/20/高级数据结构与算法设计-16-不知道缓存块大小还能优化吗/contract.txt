Static perfect BST n=2**height-1. layout returns key->address; search_trace returns logical keys. B and memory_blocks positive, offset nonnegative.
Cache counters begin empty; each full trace retains state across queries. Fixed experiment uses four block slots, M=4B, not fixed word capacity.
LRU counts are not ideal-cache OPT or real CPU cache measurements. Arrays/trace reside in Python memory.
