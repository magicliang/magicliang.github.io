2026-09-20，第16篇验证
范围：examples/distributed-systems/verify16；未修改累计raft核心或旧实验，没有创建/tmp零散实验二进制。
作者固定目录命令（examples/distributed-systems）：
GOTMPDIR="$PWD/.build/tmp" GOCACHE=/private/tmp/ds-go-cache go build -race -o .build/verify16 ./verify16
./.build/verify16 > .build/verify16-output.txt
GOCACHE=/private/tmp/ds-go-cache go vet ./verify16 ./raft
shasum -a 256 .build/verify16
最终session69447 exit0，SHA256：321ac9fd2cda6e2618d12a1704d8f2838985ec74c8d27eeee328f140ff3a7202
首次实现运行也exit0，随后增加尾随JSON拒绝并删除无用局部变量后同路径重建复跑，非换名绕过。

实际最终输出：
PASS normal vote result=EXHAUSTED_NO_COUNTEREXAMPLE visited=870 expanded=862
PASS broken vote length=5 visited=137
PASS shorter vote depth=4 visited=112 frontier=74
PASS normal election result=EXHAUSTED_NO_COUNTEREXAMPLE visited=870 expanded=862
PASS broken election length=7 visited=627
PASS shorter election depth=6 visited=548 frontier=248
PASS replay guards, normal-adapter schedule replay, budget UNKNOWN

生成7份JSON：normal-vote、normal-election、broken-vote、broken-election、shorter-vote、shorter-election、budget。
固定模型：3节点/空日志/term0或1/候选1与2各一次term0 Timeout/节点3一次恢复/事件及保存原子。
模型原子动作上界11；每个后继重放全前缀，无Node.Clone。
完整搜索状态键为精确JSON字符串，保留全部审计观察；无短指纹压缩。
程序自检：反例完整回放；非法action.Node=99拒绝；篡改最终disk.Vote=99拒绝；正常适配器同selector重放无目标违反；L-1深度搜索无反例；stateBudget=1输出UNKNOWN。
原始JSON附同目录，回放不验证Visited/Expanded/Frontier或最短性统计；这些由实际BFS与L-1检验支持。

未运行TLC或其他证明工具；无真实socket、磁盘故障、线程并发、活性、公平性验证。
此前旧篇受限二进制未重试。本篇未执行ds13-parent-check/raft11，未提交push。
父独立CLI/回放及站点验收后续补录。

写作：blog-editor与blog-illustrator入口；父已全文审阅临时稿，无阻断。落盘前先anti-ai-tone材料/段落/节奏/连续性人工四遍及tech检查errors0 warnings0，随后anti-persona人工审阅与regex无命中，再落正文。6张机制图，2练习；图中实际轨迹为T1,T2,RV1到3,恢复3,RV2到3,回复3到1,回复3到2，第6步才首次当选。7个JSON从本次实际输出原样复制，同名目录永久保存；正文直接链接两条反例。

父独立验收：复用同SHA321ac9fd2cda6e2618d12a1704d8f2838985ec74c8d27eeee328f140ff3a7202，未重新编译。
完整运行session40083 exit0，全部统计一致：正常870/862、坏双投5/137、L4=112/74、坏双当选7/627、L6=548/248。
七份JSON与作者产物逐字一致。
CLI session31671：帮助exit0、broken-election回放exit0；尾随第二JSON、Node99篡改、未知flag、多余位置参数均exit2。
父后续站点构建/浏览器验收待补。

附件修复：父Hexo首次构建exit0（2314文件/6.49秒），但.json被渲染成网页，原链接无原始文件。7个源附件重命名.json.txt并同步正文两href；内容不变，-replay不限制后缀，.build生成.json不变，未修改全站配置/public。此为局部链接与说明修复，不属新增散文，按AGENTS触发规则豁免双扫描。父已确认6图均生成SVG且无错误，七步时序截图可读；修复后重建待父。

父最终站点验收：修复后npm run build exit0，2308文件/5.44秒，日志/private/tmp/ds-build-16-final.log。9个附件与源文件逐字一致；七份JSON内容亦与父独立搜索产物逐字相同。前篇15生成目标存在，front matter/封面检查通过。浏览器修复后链接为broken-election.json.txt，6张SVG无mermaid-error，动作枚举与七步时序截图文字可读。未手改public，未部署。
