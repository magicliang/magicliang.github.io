#include <stdint.h>
#include <stdio.h>

int main(void) {
    volatile uint32_t input[] = {3, 4, 5, 6, 7};
    uint32_t sum = 0;
    for (unsigned i = 0; i < 5; i++) sum += input[i];
    printf("sum=%u\n", sum);
    return sum == 25 ? 0 : 1;
}
