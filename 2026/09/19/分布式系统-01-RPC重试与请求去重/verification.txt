分布式系统 01 本地验证记录
日期 2026-09-19；Go 1.27.0 darwin/arm64；不引入第三方Go依赖。
实验目录 examples/distributed-systems，代码 rpc01/main.go。

GOCACHE=/private/tmp/ds-go-cache go run -race ./rpc01 -check
退出0，实际输出：
reply-loss dedupe=false balance=2 executions=2
reply-loss dedupe=true balance=1 executions=1
concurrent calls=32 balance=1 executions=1 replayed-result=1
parameter-conflict rejected=true balance=1
new-id balance=2 old-id-replay=1
cache-lost-after-restart balance=3 duplicate-applied=true
CHECK passed

父线程独立读取代码、运行同一命令和go vet ./rpc01，均退出0。
本执行者再次在模块目录运行go vet ./rpc01，退出0，无诊断。
GOCACHE=/private/tmp/ds-go-cache go run -race ./rpc01 -help：退出0，显示-check帮助。
GOCACHE=/private/tmp/ds-go-cache go run -race ./rpc01 unexpected：
输出unexpected positional argument / exit status 2；go run外层退出1，错误路径符合预期。

变异检查：仅/private/tmp/rpc01-mutant.go临时副本，将返回old.Result改为s.balance。
go run -race执行退出1，panic: retry returned current rather than original result。
证明内置检查会识别回放当前值这一错误；正式代码未被变异。
front matter、同名evidence/verification附件、已有封面路径检查通过；diff --check通过。

证据边界 VERIFIED_LOCAL：真实Go方法、goroutine并发、内置断言、race检查；
消息丢失是deliver在执行后丢弃结果；重启是创建新对象并仅转移业务值。
不是TCP网络端到端实验，不是操作系统杀进程恢复，不是磁盘持久化检查。
没有实际gRPC运行、TTL/租约实现、外部副作用、吞吐/时延评测或Go1.23兼容运行。
全站npm构建和浏览器预览由父线程在整合后负责，未在本记录先行宣称通过。
