06 验证记录
日期：2026-09-19；Go go1.27.0 darwin/arm64。
负责范围：本篇正文、同名素材目录、examples/distributed-systems/consistency06/。
未提交、未推送、未部署；全站构建与集成提交由父代理负责。

写作流程：读取blog-editor、创作流程及两项扫描skill。
正文先保存在/tmp/consistency06-draft.md，完成来源与段落审阅。
按顺序：anti-ai-tone --profile tech（0错误0提醒）→anti-persona-fabrication词项扫描（0命中）及人工三红线/十类检查→落盘source。
全文152行，约4300汉字；复用系列封面，不添加外部图片。
父代理独立阅读全文，修正01post_link并明确因果视图例子后通过。

运行目录：examples/distributed-systems
默认Go缓存写入被沙箱拒绝，改用可写/tmp缓存，未更换认证/权限。
GOCACHE=/tmp/hexo-consistency06-go-cache go run -race ./consistency06
exit 0，实际输出：
PASS overlap          linear=true witness=[r w] sequential=true witness=[r w]
PASS sequential-only  linear=false witness=[] sequential=true witness=[r w]
PASS program-order    linear=false witness=[] sequential=false witness=[]
PASS cycle            linear=false witness=[] sequential=false witness=[]
PASS invented-value   linear=false witness=[] sequential=false witness=[]
PASS mutation: deleting order edges falsely accepts 3 negative histories
PASS guards: pending, overlap, duplicate IDs/events, >8 rejected; empty/8/reordered accepted
GOCACHE=/tmp/hexo-consistency06-go-cache go vet ./consistency06
exit 0，无输出。
GOCACHE=/tmp/hexo-consistency06-go-cache go run ./consistency06 -scenario sequential-only
exit 0，符合上表该场景。
父代理独立race与vet：已收到exit0确认；非本记录作者冒认执行。

负向源码变异：将临时副本中“if !ignoreOrder &&”替换为“if false &&”，其他不变。
GOCACHE=/tmp/hexo-consistency06-go-cache go run /tmp/consistency06-mutant.go
go run exit1，程序panic: wrong verdict for sequential-only，并显示exit status2。
证明断言能捕获实时序约束被删，变异文件未加入仓库。

额外CLI检查限制：go run -scenario unknown与-help均出现signal:killed；随后go build -o /tmp/consistency06-checker成功，但该临时二进制无参数、help、unknown及位置参数均直接exit137，无程序输出。
原因未定位；不能把exit137当预期退出码2。CLI帮助/错误处理只做源码检查，暂未形成运行证据。父代理可在其独立执行环境补验。

静态核对：front matter、同名目录、封面现存、post_link对应01/05文章、附件target=_blank rel=noopener；git diff --check作用于归属路径无报错。
验证边界：有限完整历史的纯内存DFS，不是真实KV/分布式端到端；没有网络、磁盘、时钟、服务活性或性能实测。pending不支持；因果一致性与最终收敛未实现检查器。
最低Go1.23未实际运行；全站npm run build待父集成。

父任务独立复核：完整阅读正文与源码，race五样例/边界/内置变异和vet退出0。另独立核对2021修订与CAP原文。go run -race -scenario unknown 实际输出 unknown scenario: unknown、exit status 2，外层exit1，符合预期。go run -race -help仍signal:killed；改为 go build -race -o /private/tmp/ds06-parent-check ./consistency06 后直接 /private/tmp/ds06-parent-check -help，显示全部scenario用法并exit0。因此help和未知参数最终验证通过，但环境间歇终止根因未定位，历史失败保留。
